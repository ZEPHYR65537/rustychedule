# v0.1.0 验证记录

日期：2026-09-21。执行环境：Windows x64 / Rust 1.94.0。

| 检查 | 结果 |
|---|---|
| `cargo fmt --check` | 通过 |
| `cargo test --locked` | 37 项通过：31 项领域测试、5 项真实 CLI 流程测试、1 项终端字符宽度测试 |
| `cargo clippy --locked --all-targets -- -D warnings` | 通过，无警告 |
| `cargo build --release --locked` | 通过，生成 `tongchou.exe` |
| 发布程序实际演示 | 初始化、四象限、分段降级规划与提交、额度条、每模型统计、数据验证通过 |

核心回归覆盖：模型偏序传递和互不可比、循环检测、重要任务优先、强模型耗尽后的倍率降级、禁止分段、能力底线、逐模型额度隔离、多窗口限制、预算重复提交、进度与消耗分离、重规划范围、失败试分配回滚、取整不超限、三类重置、过期卡与范围限制、滚动快照到期、同时间戳事件排序、错误用量撤销、原子保存、并发锁和损坏文件恢复。

恢复测试还验证：损坏原件不会覆盖上一份可用备份，恢复后原件保存在 `state.json.corrupt`。

仓库 CI 为 Windows、Linux、macOS 分别运行格式、测试、静态检查与发布构建，并上传可执行文件。此文记录本机已实际完成的验证；远程 CI 结果以 GitHub Actions 为准。

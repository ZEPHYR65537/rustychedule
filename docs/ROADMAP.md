# 后续迭代

## v0.2 已实现

独立订阅/独立 API token 上限、周容量手动估计与原始百分比、月续期提示、三种来源策略、全局及任务偏序、时间与成果复盘、项目注册/记忆、Git 中心仓库同步、代码引用及本地项目 bundle、中文终端视图、v1 迁移和 schedule/tc 命名。

## 当前边界

- 不记录价格、金额或费用；只管理用量。
- 不调用模型、不购买 tokens、不执行厂商账户重置；外部已经发生的用量/刷新由用户登记。
- 规划使用当前额度和一次连续时间段，贪心不保证全局最优。
- 周容量和模型倍率靠人工估计，无自动学习或预测。
- Git 账号只用于生成中心仓库地址，用户预先建私有仓库并配置 Git 认证。
- 多设备采用交接式单写端；双端离线修改拒绝自动合并。
- 代码 reference 保留于原仓库，未推送代码不跟随 hub；snapshot 不含未提交/忽略文件、其他分支、子模块或 LFS 实体。
- 任务/配置保存现值，用量/校准/刷新保留流水；还不是全事件溯源系统。

## 下一版本：人工管理体验

1. 分阶段任务、交接包、独立输入/输出估计及质量验收点。
2. 可配置优先级权重、冻结预算、计划版本及前后差异。
3. 卡库存纠错流水、估计配置历史、项目注销/改名和安全迁移。
4. shell 补全、交互式初始化、CSV、ASCII 视图和窄终端模式。
5. 同步状态差异与冲突选择向导；可选 GitHub CLI 私有仓库创建，不保存密码。

## 预测设计（尚未实现）

1. 建立 EstimateRevision，记录估计值、生效时间、来源、置信区间。
2. 结合面板百分比差和已知真实使用，排除跨重置、缺失记录和外部消耗区间，稳健估计每模型周容量。
3. 根据任务特征、阶段、模型、上下文、复盘记录预测 token 分布；人工覆盖优先。
4. 将不确定性转为保守规划余量，不将 token 使用比当作成果比。
5. Tibo 可做历史统计，但可执行计划默认继续不依赖随机未来刷新。

## 时间、多设备与自动采集

1. 纳入已知未来自然重置、多天日程、工作空档；随机 Tibo 不提供保证容量。
2. 引入全局唯一 ID、事件幂等键、实体版本和明确冲突规则，再启用多端离线合并。
3. 厂商只读用量采集写入同一事件层，保留人工校准优先级与每模型/来源维度。
4. 可选整数规划求解器，明确任务优先、模型偏好、估计风险的目标顺序。
5. 如果未来提供真实重置适配器，必须有用户显式授权、可恢复执行记录；不能把当前的本地登记命令悄悄改成远程重置。

任何迭代都必须遵守 DECISIONS.md，改变数学语义同步更新 MATHEMATICS.md，数据不兼容时增加迁移与回归测试。

# Changelog

## 0.3.0 · 2026-09-21

- 将逻辑任务规划与 agent 容器分离，支持项目/文件夹/聊天到任务的多对多关联。
- 新增 agent 树、普通目录整体纳管、嵌套 Git 仓库边界、文件排除规则和 UTF-8 正文搜索。
- 仓库索引支持远程地址或机器 ID＋本地路径；机器别名可修改，代码不复制入中央。
- 文件收集根据基准检测双方修改；源缺失不删除，显式 prune 才传播无冲突删除；支持恢复原目录结构。
- 支持文件夹移动、改名、拆分、保留来源分组的合并、归档、中央节点删除与 Git 恢复。
- 新 hub 格式 2 中元信息/正文可直接编辑；原子文件事务、hub 锁和中断恢复。
- 当前分支 Git 同步、临时工作树预检、目录语义校验、按项目/节点选择正文、可选 partial clone。
- agents-only 同步不改本机规划账本；保留每模型与额度来源独立约束，规划 State 仍为 v2。
- 旧 hub 显式迁移，保存 legacy/v02 文件和 bundle；其他旧设备跟随中央身份升级。
- 新增落地设计、用户操作指南、完整 CLI/结构说明及跨设备回归测试。

## 0.2.0 · 2026-09-21

- 包名/主命令改为 schedule，增加短命令 tc，保留 tongchou 兼容入口。
- 每模型订阅、估计周 token 容量、原始百分比、月续期提示与独立 API token 累计上限。
- API 上限为 0 禁用补充；正数仅在本模型剩余上限内预留。所有订阅刷新均不影响 API。
- 新增 subscription-first、model-first、subscription-only 来源策略和全局模型偏序。
- 同一模型订阅+API 可以共同承担不可跨模型分段的任务。
- 移除所有单价、货币、金额与费用计算/字段，仅管理用量。
- 新增实际工作时间、完成内容、收获和下一步复盘，关联单一用量事件。
- 新增本地项目注册、代码状态汇总、上下文/长期记忆；已有代码保留原仓库，无远程项目可制作 Git bundle。
- 显式 Git 工作空间同步、单写端交接、双端修改冲突保护和本机路径隔离。
- 新增落地设计、完整命令清单、数据结构、数学约束、迁移文档；schema v1→v2 自动解码迁移。
- 更新 Windows/Linux/macOS CI，构建 schedule、tc 与兼容程序。

## 0.1.0 · 2026-09-21

历史首版（部分设计已由 0.2.0 替代）：

- 发布 Rust CLI tongchou，支持中文终端可视化与 JSON。
- 任务重要性/紧迫性、截止日期、项目标签、依赖、状态、手动进度。
- 每模型独立多窗口额度；当时包含的价格/费用字段已在 0.2.0 按用户要求移除。
- 任务模型偏序、能力底线、允许列表、倍率、分段降级、预算重规划。
- 用量记账、校准、撤销、自然重置、Tibo、reset 卡。
- 文件锁、原子保存、备份恢复、数据验证与数学/设计文档。
